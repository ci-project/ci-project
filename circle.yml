dependencies:
  pre:
    - curl -O https://googleappengine.googlecode.com/files/google_appengine_1.7.4.zip
    - unzip -d $HOME google_appengine_1.7.4.zip

test:
  override:
    - python -m runtests

deployment:
  appengine:
    branch: master
    commands:
      - $HOME/google_appengine/appcfg.py --oauth2_refresh_token=1/5dpDAWoe3xecNf15jr7GpmfuvfAA8yDS7lUnxqHX0-w update .