dependencies:
  pre:
    - curl -O https://googleappengine.googlecode.com/files/google_appengine_1.7.4.zip
    - unzip -d $HOME google_appengine_1.7.4.zip

test:
  override:
    - python -m runtests